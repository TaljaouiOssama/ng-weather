import { WeatherImageDirective } from './weather-image.directive';

describe('WeatherImageDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherImageDirective();
    expect(directive).toBeTruthy();
  });
});
